# Build command (astro build) and publish (dist) are auto-detected for Astro.
# See https://docs.netlify.com/build/frameworks/framework-setup-guides/astro/

[build.environment]
  NODE_VERSION = "22"
  # APP_PASSWORD and WRITE_TOKEN: set in Netlify UI (Site configuration → Environment variables).
  # Do not commit secrets here. See .env.example.
